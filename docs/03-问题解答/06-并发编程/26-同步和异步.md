# 同步和异步详解
> 📖 相关章节：[多线程基础](../../02-CPP/29-多线程基础.md)、[线程同步](../../02-CPP/30-线程同步.md)、[原子操作](../../02-CPP/31-原子操作与异步编程.md)、[无锁编程](../../02-CPP/33-无锁编程.md)

## 1. 什么是同步和异步？

### 1. 通俗理解

想象你去餐厅点餐：

**同步（Synchronous）**：
- 你点完餐后站在柜台前等，直到服务员把餐做好给你
- 在这期间你不能做其他事情，只能等待
- **特点**：阻塞、顺序执行、简单直接

**异步（Asynchronous）**：
- 你点完餐后拿到一个号码牌，回到座位玩手机
- 餐做好了服务员会叫你的号码
- **特点**：非阻塞、并发执行、效率高但复杂

---

## 2. 同步与异步的实现方式

### 1. 同步实现

#### 1. （1）互斥锁（Mutex）

```cpp
#include <mutex>
#include <thread>
#include <iostream>

std::mutex mtx;
int shared_data = 0;

void safe_increment() {
    std::lock_guard<std::mutex> lock(mtx);  // 加锁
    shared_data++;  // 临界区：同一时间只有一个线程能执行
}  // 自动解锁

int main() {
    std::thread t1(safe_increment);
    std::thread t2(safe_increment);
    t1.join();
    t2.join();
    std::cout << "Result: " << shared_data << std::endl;
    return 0;
}
```

**核心**：同一时间只允许一个线程访问共享资源。

#### 2. （2）条件变量（Condition Variable）

```cpp
#include <mutex>
#include <condition_variable>
#include <thread>
#include <iostream>

std::mutex mtx;
std::condition_variable cv;
bool ready = false;

void worker() {
    std::this_thread::sleep_for(std::chrono::seconds(2));
    {
        std::lock_guard<std::mutex> lock(mtx);
        ready = true;
    }
    cv.notify_one();  // 通知等待的线程
}

int main() {
    std::thread t(worker);

    {
        std::unique_lock<std::mutex> lock(mtx);
        cv.wait(lock, []{ return ready; });  // 等待条件满足
    }

    std::cout << "Worker is done!" << std::endl;
    t.join();
    return 0;
}
```

#### 3. （3）原子操作（Atomic）

```cpp
#include <atomic>
#include <thread>
#include <iostream>

std::atomic<int> counter(0);

void increment() {
    for (int i = 0; i < 1000; ++i) {
        counter.fetch_add(1);  // 原子操作，线程安全
    }
}

int main() {
    std::thread t1(increment);
    std::thread t2(increment);
    t1.join();
    t2.join();
    std::cout << "Counter: " << counter.load() << std::endl;  // 2000
    return 0;
}
```

---

### 2. 异步实现

#### 1. （1）多线程 / 线程池

```cpp
#include <thread>
#include <iostream>
#include <chrono>

void async_task() {
    std::this_thread::sleep_for(std::chrono::seconds(2));
    std::cout << "Async task completed" << std::endl;
}

int main() {
    // 异步执行：主线程不等待
    std::thread t(async_task);
    t.detach();  // 分离线程

    std::cout << "Main thread continues..." << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(3));  // 等待子线程完成

    return 0;
}
```

#### 2. （2）Future / Promise

```cpp
#include <future>
#include <thread>
#include <iostream>

int compute() {
    std::this_thread::sleep_for(std::chrono::seconds(2));
    return 42;
}

int main() {
    // 异步执行
    std::future<int> fut = std::async(std::launch::async, compute);

    // 主线程可以做其他事情
    std::cout << "Doing other work..." << std::endl;

    // 需要结果时才获取（会阻塞）
    int result = fut.get();
    std::cout << "Result: " << result << std::endl;

    return 0;
}
```

#### 3. （3）回调函数

```cpp
#include <thread>
#include <functional>
#include <iostream>

void async_operation(std::function<void(int)> callback) {
    std::thread t([callback]() {
        std::this_thread::sleep_for(std::chrono::seconds(1));
        callback(100);  // 调用回调传递结果
    });
    t.detach();
}

int main() {
    async_operation([](int result) {
        std::cout << "Callback received: " << result << std::endl;
    });

    std::cout << "Main continues..." << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(2));

    return 0;
}
```

---

## 3. 同步与异步的应用场景

### 1. 同步适用场景

| 场景 | 说明 |
|------|------|
| 多线程修改共享变量 | 必须加锁保护，避免数据竞争 |
| 操作文件、数据库 | 需要保证操作顺序和数据一致性 |
| 需要严格执行顺序 | 后续步骤依赖前一步的结果 |
| 简单的任务流程 | 逻辑清晰，不需要复杂并发 |

### 2. 异步适用场景

| 场景 | 说明 |
|------|------|
| 网络请求 | HTTP请求、WebSocket通信 |
| 文件读写 | 大文件IO操作 |
| 耗时计算 | 图像处理、数据分析 |
| UI响应 | 不能卡住用户界面 |
| 高并发服务 | Web服务器、游戏服务器 |

---

## 4. 技巧与注意事项

### 1. 同步注意事项

#### 1. 锁粒度越小越好

**锁粒度** = 你加锁包住的代码范围、执行时间。

```cpp
// ❌ 错误：锁粒度太大
void bad_example() {
    std::lock_guard<std::mutex> lock(mtx);
    do_network_request();  // 耗时操作不应该在锁内
    complex_computation();  // 耗时操作不应该在锁内
    modify_shared_data();   // 真正需要锁的只有这一句
}

// ✅ 正确：锁粒度最小
void good_example() {
    do_network_request();   // 锁外执行
    complex_computation();  // 锁外执行

    {
        std::lock_guard<std::mutex> lock(mtx);
        modify_shared_data();  // 只锁这一行
    }
}
```

**原因**：
- 减少线程阻塞等待时间
- 提升并发性能
- 降低死锁概率
- 避免把无关业务卡在锁内

#### 2. 避免嵌套锁，防止死锁

```cpp
// ❌ 危险：可能导致死锁
void dangerous() {
    std::lock_guard<std::mutex> lock1(mtx1);
    std::lock_guard<std::mutex> lock2(mtx2);  // 可能死锁
}

// ✅ 安全：使用 std::scoped_lock
void safe() {
    std::scoped_lock lock(mtx1, mtx2);  // 同时加锁，避免死锁
}
```

#### 3. 不要在锁里做耗时操作

```cpp
// ❌ 错误
void bad() {
    std::lock_guard<std::mutex> lock(mtx);
    sleep(10);  // 持有锁10秒，其他线程全部阻塞
}

// ✅ 正确
void good() {
    auto data = prepare_data();  // 锁外准备
    {
        std::lock_guard<std::mutex> lock(mtx);
        save_data(data);  // 锁内只做关键操作
    }
}
```

---

### 2. 异步注意事项

#### 1. 子线程一般不能直接更新 UI

**原因**：几乎所有 UI 框架（Qt、WinForm、WPF、iOS、Android）都规定 UI 控件不是线程安全的，只允许主线程（UI 线程）操作。

```cpp
// ❌ 错误：子线程直接更新 UI
void worker() {
    label.setText("Done");  // 可能导致崩溃
}

// ✅ 正确：通过信号槽或事件队列通知主线程
void worker() {
    emit signal_done();  // Qt 信号槽机制
}
```

#### 2. 避免过多回调嵌套（回调地狱）

```cpp
// ❌ 错误：回调地狱
async_op1([](result1) {
    async_op2(result1, [](result2) {
        async_op3(result2, [](result3) {
            // ... 无限嵌套
        });
    });
});

// ✅ 正确：使用 async/await 或 future 扁平化
auto r1 = co_await async_op1();
auto r2 = co_await async_op2(r1);
auto r3 = co_await async_op3(r2);
```

#### 3. 异步必须捕获异常

**原因**：主线程 try-catch 抓不到子线程异步里的异常。

```cpp
// ❌ 错误：未捕获异常
void worker() {
    throw std::runtime_error("Error");  // 无人处理，程序崩溃
}

// ✅ 正确：异步内部捕获异常
void worker() {
    try {
        // 异步任务
    } catch (const std::exception& e) {
        std::cerr << "Error: " << e.what() << std::endl;
    }
}
```

#### 4. 异步修改共享数据仍需要同步锁

**误区**：很多人以为用异步、多线程跑，就不用管锁了。

**真相**：异步本质还是多线程并发，多个异步任务同时读写同一个全局变量依然存在竞态条件。

```cpp
// ❌ 错误：异步中未加锁
std::vector<int> shared_data;

void async_task() {
    shared_data.push_back(42);  // 数据竞争！
}

// ✅ 正确：异步中仍需加锁
std::mutex mtx;
std::vector<int> shared_data;

void async_task() {
    std::lock_guard<std::mutex> lock(mtx);
    shared_data.push_back(42);  // 线程安全
}
```

#### 5. 不要无限创建线程，要用线程池

**为什么不能无限 new 线程？**

1. **线程资源昂贵**：每个线程占栈内存、内核 PCB 资源
2. **CPU 上下文切换爆炸**：线程远超 CPU 核心数，大量时间浪费在切换
3. **不可控**：任务多就疯狂建线程，系统卡顿、OOM、程序直接挂掉

```cpp
// ❌ 错误：无限创建线程
for (int i = 0; i < 10000; ++i) {
    std::thread t(task);  // 创建10000个线程，系统崩溃
    t.detach();
}

// ✅ 正确：使用线程池
#include <thread>
#include <vector>

const int MAX_THREADS = 8;
std::vector<std::thread> pool;

for (int i = 0; i < MAX_THREADS; ++i) {
    pool.emplace_back(task);
}
for (auto& t : pool) {
    t.join();
}
```

---

## 5. 总结

### 3. 同步 vs 异步对比

| 特性 | 同步 | 异步 |
|------|------|------|
| **执行方式** | 顺序执行 | 并发执行 |
| **阻塞** | 阻塞等待 | 非阻塞 |
| **复杂度** | 简单 | 复杂 |
| **性能** | 较低 | 较高 |
| **适用场景** | 简单任务、共享数据保护 | IO密集型、高并发 |

### 4. 核心原则

1. **锁粒度越小越好**：快进快出、少阻塞、高并发、少死锁
2. **异步五条本质**：
   - UI 只能主线程碰
   - 少嵌套，用 async/await
   - 异步必捕异常
   - 共享数据照样要加锁
   - 不裸开线程，统一用线程池

---

## 5. 极简口诀

```
同步排队等结果，异步后台跑任务
锁住共享保安全，粒度越小越高效
异步注意五要点，线程池来管并发
UI更新回主线，异常捕获不能忘
```

***

### 相关阅读

- [并发并行异步同步辨析](./24-并发并行异步同步辨析.md)
- [什么是future与promise](./28-什么是future与promise.md)
- [多线程通讯与数据获取](./14-多线程通讯与数据获取.md)